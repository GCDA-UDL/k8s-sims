# scripts excluded from the bash test suite inventory
# one relative path per line, comments start with #
# entries here MUST NOT be re-included via the glob inventory

# Template module provided for copy/paste; not a runnable module
modules/template/module.sh
