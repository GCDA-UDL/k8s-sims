SHIM_RULES=(
  "image pull:WARNING: simulated pull failure|0"
  "ps --no-trunc:"
  "logs:"
  "compose up:"
  "compose down:"
  "rm -f:"
  "ps -a --filter:"
  "rm -f $containers:"
)
DEFAULT_EXIT_CODE=0
